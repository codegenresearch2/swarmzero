import pytest\\nfrom swarmzero.chat import ChatManager\\n\\n@pytest.fixture\\ndef chat_manager()-\\n    return ChatManager()\\n\\n@pytest.mark.asyncio\\nasync def test_add_message(chat_manager):\\n    await chat_manager.add_message('user1', 'session1', 'Hello!', 'user')\\n    messages = await chat_manager.get_messages()\\n    assert len(messages) == 1\\n    assert messages[0]['content'] == 'Hello!'\\n\\n@pytest.mark.asyncio\\nasync def test_generate_response(chat_manager):\\n    user_message = {'role': 'user', 'content': 'Hello!'}\\n    response = await chat_manager.generate_response(user_message)\\n    assert response == 'chat response'\\n